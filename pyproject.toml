[project]
name = "celery-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.125.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.9",
]
